# PR18 — QA review / approval / locking / e-signatures

## Scope
Implement QA review, approval, controlled locking and electronic-signature workflow for critical records. Separate preparation from approval and prevent ordinary mutation after controlled lock.

## Dependency
PR17 must be merged first. Acceptance: separation of duties, signature intent, signer identity/time, lock enforcement, amendment/version path, audit events, security tests.

## Production rule
This PR is a gated production increment. It must not weaken tenant isolation, RBAC, auditability, data integrity, or server-side authority to make tests pass.

## Verification
- python manage.py check
- python manage.py makemigrations --check
- pytest
