# PR21 — Reports / exports / authorization

## Scope
Implement controlled report generation and exports from authorized data only. Apply tenant/site/RBAC checks to report creation, retrieval and export; preserve report provenance and version context.

## Dependency
PR20 must be merged first. Acceptance: no cross-tenant export, permission enforcement, reproducible report metadata, background-job authorization, file access tests.

## Production rule
This PR is a gated production increment. It must not weaken tenant isolation, RBAC, auditability, data integrity, or server-side authority to make tests pass.

## Verification
- python manage.py check
- python manage.py makemigrations --check
- pytest
