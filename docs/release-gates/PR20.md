# PR20 — Audit completeness / integrity / controlled files

## Scope
Complete audit coverage for critical domain actions and establish controlled file metadata/access behavior. Audit records must capture actor, timestamp, action, object, before/after or meaningful change context where applicable, and remain protected from ordinary mutation.

## Dependency
PR19 must be merged first. Acceptance: audit coverage matrix, protected audit writes, file tenant/site authorization, download controls, integrity tests, retention/documentation boundaries.

## Production rule
This PR is a gated production increment. It must not weaken tenant isolation, RBAC, auditability, data integrity, or server-side authority to make tests pass.

## Verification
- python manage.py check
- python manage.py makemigrations --check
- pytest
