# PR22 — Plans / Entitlements / Usage / Subscriptions / Invoices

## Scope
Implement the commercial domain: plans, entitlements, usage metering, subscriptions and invoices. Keep billing state server-authoritative and separate from application permissions.

## Dependency
PR21 must be merged first. Acceptance: tenant isolation, deterministic entitlement evaluation, usage accounting, invoice lifecycle, idempotent internal operations, billing tests.

## Production rule
This PR is a gated production increment. It must not weaken tenant isolation, RBAC, auditability, data integrity, or server-side authority to make tests pass.

## Verification
- python manage.py check
- python manage.py makemigrations --check
- pytest
