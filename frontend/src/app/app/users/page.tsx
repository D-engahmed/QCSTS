"use client";import {useEffect,useState} from "react";import {api,endpoints,type ApiEnvelope} from "@/lib/api";import type {User} from "@/lib/auth";
export default function Users(){const [users,setUsers]=useState<User[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);const load=()=>{setLoading(true);api<ApiEnvelope<User[]>>(endpoints.users).then(r=>setUsers(r.data||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);return <div className="content"><div className="page-header"><div><span className="eyebrow">ADMINISTRATION</span><h1>Users & memberships</h1><p>Members are scoped to the active organization. The backend decides permissions and membership state.</p></div></div>{error&&<div className="inline-error">{error}</div>}<section className="card table-card"><div className="card-header"><div><strong>Organization members</strong><span>{users.length} members returned by the API</span></div><button className="btn" onClick={load}>Refresh</button></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Organization role</th><th>Status</th><th>Created</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><b>{u.full_name}</b></td><td>{u.email}</td><td>{u.organization_role||"—"}</td><td><span className={"status "+(u.is_active?"active":"critical")}>{u.is_active?"Active":"Inactive"}</span></td><td>{new Date(u.created_at).toLocaleDateString()}</td></tr>)}</tbody></table>{loading&&<div className="empty">Loading members…</div>}{!loading&&!users.length&&!error&&<div className="empty">No members were returned for this organization.</div>}</div></section></div>}
