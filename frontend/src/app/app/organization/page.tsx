"use client";import {useAuth} from "@/components/AuthProvider";
/** Displays details for the active organization and its available sites. */
export default function Organization(){const {organization,sites}=useAuth();return <div className="content"><div className="page-header"><div><span className="eyebrow">TENANT</span><h1>{organization?.name||"Organization"}</h1><p>Organization and site context sent with protected API requests.</p></div></div><div className="grid-2"><section className="card detail-card"><h3>Organization</h3><dl><dt>Legal name</dt><dd>{organization?.legal_name||"—"}</dd><dt>Country</dt><dd>{organization?.country||"—"}</dd><dt>Timezone</dt><dd>{organization?.timezone||"—"}</dd><dt>Currency</dt><dd>{organization?.currency||"—"}</dd><dt>Status</dt><dd>{organization?.status||"—"}</dd></dl></section><section className="card detail-card"><h3>Sites</h3>{sites.map(s=><div className="site-row" key={s.id}><b>{s.name}</b><span>{s.address||s.country}</span><span className="status active">{s.status}</span></div>)}{!sites.length&&<div className="empty">No sites are available.</div>}</section></div></div>}
