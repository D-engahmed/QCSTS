"use client";
import { useState } from "react"; import { ShieldCheck } from "lucide-react";
export default function Login(){const [busy,setBusy]=useState(false);return <main className="auth"><div className="auth-card"><div className="brand auth-brand"><div className="brand-mark">Q</div><div><strong>QCSTS</strong><small>Quality & Stability</small></div></div><h1>Sign in</h1><p>Access your controlled pharmaceutical quality workspace.</p><form onSubmit={e=>{e.preventDefault();setBusy(true);window.location.href="/"}}><label className="field"><span>Work email</span><input type="email" required placeholder="name@company.com"/></label><label className="field"><span>Password</span><input type="password" required/></label><button className="btn primary full" disabled={busy}>{busy?"Signing in…":"Sign in"}</button></form><div className="security-note"><ShieldCheck size={15}/><span>Authentication, sessions and organization access are controlled server-side.</span></div></div></main>}
