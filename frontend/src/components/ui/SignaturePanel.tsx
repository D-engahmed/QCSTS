"use client";import {useState} from "react";
export function SignaturePanel({action,onCancel,onConfirm,requiresReason=true,pending=false}:{action:string;onCancel:()=>void;onConfirm:(reason:string,password:string)=>void;requiresReason?:boolean;pending?:boolean}){const [password,setPassword]=useState(""),[reason,setReason]=useState("");const can=!!password&&(!requiresReason||!!reason.trim());return <div className="modal-backdrop"><section className="signature-panel" role="dialog" aria-modal="true" aria-labelledby="signature-title"><span className="eyebrow">CONTROLLED ACTION</span><h2 id="signature-title">{action}</h2><p>Re-confirm your identity before recording this regulated action.</p><label className="field"><span>Password</span><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label>{requiresReason&&<label className="field"><span>Reason</span><textarea required value={reason} onChange={e=>setReason(e.target.value)} placeholder="Enter the reason for this action"/></label>}<div className="modal-actions"><button className="btn" onClick={onCancel} disabled={pending}>Cancel</button><button className="btn primary" disabled={!can||pending} onClick={()=>onConfirm(reason,password)}>{pending?"Recording…":action}</button></div></section></div>}
