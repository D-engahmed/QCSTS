"use client";import {useEffect,useMemo,useState} from "react";import {StatusBadge} from "./StatusBadge";
type Col={key:string;label:string;render?:(row:any)=>React.ReactNode;sortable?:boolean};
export function DataTable({columns,rows,emptyTitle="No records",emptyText="There are no records to display.",rowHref,onRowClick}:{columns:Col[];rows:any[];emptyTitle?:string;emptyText?:string;rowHref?:(r:any)=>string;onRowClick?:(r:any)=>void}){const [sort,setSort]=useState<string>(""),[asc,setAsc]=useState(true),[density,setDensity]=useState<"comfortable"|"compact">("comfortable");useEffect(()=>{const v=localStorage.getItem("qc_table_density") as any;if(v==="compact"||v==="comfortable")setDensity(v)},[]);const changeDensity=(v:any)=>{setDensity(v);localStorage.setItem("qc_table_density",v)};const data=useMemo(()=>{if(!sort)return rows;return [...rows].sort((a,b)=>String(a[sort]??"").localeCompare(String(b[sort]??""))*(asc?1:-1))},[rows,sort,asc]);return <div className="data-table"><div className="table-tools"><div className="filter-placeholder">Filter results</div><div className="density"><button className={density==="comfortable"?"selected":""} onClick={()=>changeDensity("comfortable")}>Comfortable</button><button className={density==="compact"?"selected":""} onClick={()=>changeDensity("compact")}>Compact</button></div></div><div className="table-wrap"><table className={density}><thead><tr>{columns.map(c=><th key={c.key}>{c.sortable?<button className="sort-button" onClick={()=>{setSort(c.key);setAsc(sort===c.key?!asc:true)}}>{c.label}</button>:c.label}</th>)}</tr></thead><tbody>{data.map((r,i)=><tr key={r.id||i} tabIndex={0} onClick={()=>onRowClick?.(r)} onKeyDown={e=>{if(e.key==="Enter")onRowClick?.(r)}}>{columns.map(c=><td key={c.key}>{c.render?c.render(r):c.key==="status"?<StatusBadge status={r[c.key]}/>:r[c.key]??"—"}</td>)}</tr>)}</tbody></table>{!data.length&&<div className="empty"><b>{emptyTitle}</b><span>{emptyText}</span></div>}</div></div>}
