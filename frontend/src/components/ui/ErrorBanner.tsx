export function ErrorBanner({code,message,onRetry}:{code?:string;message?:string;onRetry?:()=>void}){const copy:Record<string,string>={PERMISSION_DENIED:"You do not have permission to perform this action.",TENANT_ACCESS_DENIED:"This resource is not available in your current workspace.",SITE_ACCESS_DENIED:"This resource is not available in your current workspace.",RESOURCE_NOT_FOUND:"The requested resource could not be found.",ENTITLEMENT_EXCEEDED:"Your organization has reached the current plan limit.",INVALID_STATE_TRANSITION:"This action is not available in the current result state.",VALIDATION_ERROR:"Check the highlighted fields and correct the values."};return <div className="error-banner" role="alert"><strong>{copy[code||""]||message||"The request could not be completed."}</strong>{code==="ENTITLEMENT_EXCEEDED"&&<span>Contact an administrator about your organization plan.</span>}{onRetry&&<button className="btn" onClick={onRetry}>Retry</button>}</div>}
